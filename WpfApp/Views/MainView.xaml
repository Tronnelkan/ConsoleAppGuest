<!-- WpfApp/Views/MainView.xaml -->
<Window x:Class="WpfApp.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:helpers="clr-namespace:WpfApp.Helpers"
        Title="Main Menu" Height="600" Width="800">
    <Window.DataContext>
        <Binding />
    </Window.DataContext>
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="400" Margin="20">
            <TextBlock Text="Main Menu" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,10"/>

            <!-- Кнопки для навигации -->
            <Button Content="Register" Command="{Binding OpenRegisterCommand}" Width="200" Height="40" Margin="0,10"/>
            <Button Content="Login" Command="{Binding OpenLoginCommand}" Width="200" Height="40" Margin="0,10"/>
            <Button Content="Recover Password" Command="{Binding OpenRecoveryPasswordCommand}" Width="200" Height="40" Margin="0,10"/>
            <!-- Добавьте другие необходимые кнопки -->

            <!-- Logout Button -->
            <Button Content="Logout" Command="{Binding LogoutCommand}" Width="200" Height="40" Margin="0,30,0,0"/>
        </StackPanel>
    </Grid>
</Window>
